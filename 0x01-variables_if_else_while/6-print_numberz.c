#include <stdio.h>

/**
* main - print all single digit numbers of base 10
*
* Return: Always 0 (success)
*/

int main(void)
{
	int c;

	for (c = 0; c < 10; c++)
	{
	putchar(c);
	}
	putchar('\n');

	return(0);
}
