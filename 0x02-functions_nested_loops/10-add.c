#include "main.h"

/**
* add - function that adds two integers, and returns the result
*
* Return: the result of the addition
*/

int add(int a, int b)
{
	int RESULT;
	RESULT = a + b;

	return (RESULT);
}
